output.elasticsearch:
    hosts: ["http://{{ hostvars['el']['ansible_facts']['default_ipv4']['address'] }}"]
setup.kibana:
    host: "http://{{ hostvars['el2']['ansible_facts']['default_ipv4']['address'] }}"
filebeat.config.modules.path: ${path.config}/modules.d/*.yml